---
import SituationArchive from '../../../components/SituationArchive.astro';
import { listSituationReports } from '../../../lib/situation-data';
const reports = listSituationReports('monthly');
const { locale = 'en' } = Astro.props as { locale?: 'en' | 'zh' | 'fr' };
---
<SituationArchive kind="monthly" reports={reports} locale={locale} />
