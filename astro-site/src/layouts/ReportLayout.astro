---
// src/layouts/ReportLayout.astro
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  breadcrumbs?: Array<{ label: string; href?: string }>;
}

const { title, description, breadcrumbs = [] } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <!-- Breadcrumb -->
  {breadcrumbs.length > 0 && (
    <nav class="site-breadcrumb border-b backdrop-blur-sm sticky top-16 z-40">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3">
        <ol class="site-breadcrumb-list flex flex-wrap items-center gap-2 text-sm">
          <li>
            <a href="/" class="site-breadcrumb-link academic-kicker tracking-[0.16em] transition-colors">Home</a>
          </li>
          {breadcrumbs.map((crumb, i) => (
            <>
              <li class="site-breadcrumb-separator">/</li>
              <li>
                {crumb.href && i < breadcrumbs.length - 1 ? (
                  <a href={crumb.href} class="site-breadcrumb-link transition-colors">
                    {crumb.label}
                  </a>
                ) : (
                  <span class="site-breadcrumb-current">{crumb.label}</span>
                )}
              </li>
            </>
          ))}
        </ol>
      </div>
    </nav>
  )}

  <!-- Page content -->
  <div class="academic-shell">
    <slot />
  </div>
</BaseLayout>
